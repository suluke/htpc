# This isn't the whole config, obviously.
# Just a reminder that you might want to change the user kodi's shell
# from /bin/nologin to something usable so you can do stuff inside its
# home directory more easily
kodi:x:420:420:kodi user:/var/lib/kodi:/usr/bin/zsh
